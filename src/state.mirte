# vim: et:sta:bs=2:sw=2:ft=yaml:
includes:
  - base
  - sarah/scheduler
definitions:
  state:
    run: true
    implementedBy: tkbd.state.State
    settings:
      schedule: { type: schedule }
      scheduler: { type: scheduler }
